package algorithm.fan;

import model.entities.Rule;
import model.entities.Scheme;
import model.entities.utils.RuleUtils;

public class FanUtils {

    public void markHeadlines(Scheme scheme) {
        for (Rule rule :RuleUtils.getAllRules()) {
        }
    }


}
